import { CreatorStudioShell } from '@/components/creator-studio/CreatorStudioShell'

export function InfluencerLayout({ children }: { children: React.ReactNode }) {
  return <CreatorStudioShell>{children}</CreatorStudioShell>
}

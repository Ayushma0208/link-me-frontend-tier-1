'use client'

import { HomePlaceholders } from '@/components/dashboard/DashboardLayout'

/** @deprecated Prefer HomePlaceholders from components/dashboard — kept for imports. */
export function UserDashboard() {
  return <HomePlaceholders />
}
